<Page
    x:Class="App4.Views.SecondPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App4.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
	DataContext="{Binding SecondPageInstance, Source={StaticResource Locator}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		
		<StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
			<TextBlock Width="100" Text="Dupa" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" TextAlignment="Center"/>
			<Button HorizontalAlignment="Center" Width="150" Command="{Binding NavigateCommand}">GO First page!</Button>
		</StackPanel>
		
		<ListView Grid.Column="1" ItemsSource="{Binding Models}"/>

	</Grid>
</Page>
